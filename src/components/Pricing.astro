---
import { siteConfig } from "../config";

const PHONE = "918144991049";

const createWhatsApp = (plan) => {
  const message = encodeURIComponent(
`Hi Saurav,

I’m interested in your ${plan} plan.
Please share more details.`
  );

  return `https://wa.me/${PHONE}?text=${message}`;
};
---

<section id="pricing" class="p-6 sm:p-10 md:p-14 lg:p-20">
  <div>
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-10 lg:gap-16 items-start">

      <!-- LEFT -->
      <div class="lg:col-span-4">
        <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-6xl font-bold text-gray-900">
          Flexible Pricing.
        </h2>

        <div class="w-[70px] h-[4px] mt-2 rounded-full"
          style={`background-color: ${siteConfig.accentColor}`} />

        <p class="text-gray-500 mt-5 text-sm max-w-sm leading-relaxed">
          100+ projects delivered • Fast turnaround • Clear communication
        </p>

        <!-- TOGGLE -->
        <div class="mt-6 flex justify-center lg:justify-start">
          <div class="relative flex bg-gray-200 rounded-full p-1 w-full max-w-[260px] h-[42px]">

            <!-- SLIDER -->
            <div id="toggleSlider"
              class="absolute top-1 left-1 w-1/2 h-[calc(100%-8px)]
                     bg-white rounded-full shadow-sm transition-all duration-300">
            </div>

            <button id="perProjectBtn"
              class="relative z-10 flex-1 flex items-center justify-center text-sm font-semibold text-gray-900 transition">
              Per Project
            </button>

            <button id="monthlyBtn"
              class="relative z-10 flex-1 flex items-center justify-center text-sm font-semibold text-gray-500 transition">
              Monthly
            </button>

          </div>
        </div>
      </div>

      <!-- RIGHT -->
      <div class="lg:col-span-8">

        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">

          <!-- BASIC -->
          <div class="bg-gray-50 rounded-xl border border-gray-200 p-5 hover:shadow-lg transition">
            <h3 class="text-lg font-bold text-gray-900">Basic</h3>

            <p class="price text-xl font-bold mt-2 text-gray-800 transition-all duration-300"
               data-project="₹999+"
               data-monthly="₹8,999/mo">
              ₹999+
            </p>

            <ul class="features mt-4 space-y-3 text-sm text-gray-600"></ul>

            <a href={createWhatsApp("Basic")}
              class="mt-6 inline-block w-full text-center px-4 py-2 rounded-md bg-[#101828] text-white font-medium hover:bg-black transition">
              Get Started
            </a>
          </div>

          <!-- STANDARD -->
          <div class="relative bg-gray-50 rounded-xl border-2 p-5 pt-7 hover:shadow-xl transition"
               style={`border-color: ${siteConfig.accentColor}`}>

            <div class="absolute -top-3 left-1/2 -translate-x-1/2">
              <span class="text-xs px-3 py-1 rounded-full text-white font-medium shadow"
                style={`background-color: ${siteConfig.accentColor}`}>
                Most Popular
              </span>
            </div>

            <h3 class="text-lg font-bold text-gray-900">Standard</h3>

            <p class="price text-xl font-bold mt-2 text-gray-800 transition-all duration-300"
               data-project="₹2,999+"
               data-monthly="₹19,999/mo">
              ₹2,999+
            </p>

            <ul class="features mt-4 space-y-3 text-sm text-gray-600"></ul>

            <a href={createWhatsApp("Standard")}
              class="mt-6 inline-block w-full text-center px-4 py-2 rounded-md bg-[#101828] text-white font-medium hover:bg-black transition">
              Get Started
            </a>
          </div>

          <!-- PREMIUM -->
          <div class="bg-gray-50 rounded-xl border border-gray-200 p-5 hover:shadow-lg transition">
            <h3 class="text-lg font-bold text-gray-900">Premium</h3>

            <p class="price text-xl font-bold mt-2 text-gray-800 transition-all duration-300"
               data-project="₹7,999+"
               data-monthly="₹39,999/mo">
              ₹7,999+
            </p>

            <ul class="features mt-4 space-y-3 text-sm text-gray-600"></ul>

            <a href={createWhatsApp("Premium")}
              class="mt-6 inline-block w-full text-center px-4 py-2 rounded-md bg-[#101828] text-white font-medium hover:bg-black transition">
              Get Started
            </a>
          </div>

        </div>

        <p class="mt-6 text-sm text-gray-500">
          Final pricing depends on project scope. Custom quotes available.
        </p>

      </div>
    </div>
  </div>
</section>

<script>
  const perBtn = document.getElementById("perProjectBtn");
  const monthlyBtn = document.getElementById("monthlyBtn");
  const slider = document.getElementById("toggleSlider");

  const prices = document.querySelectorAll(".price");
  const features = document.querySelectorAll(".features");

  const featureData = {
    project: [
      ["1 design","1 revision","1–2 days delivery"],
      ["3–5 designs / 1 video","2–3 revisions","Priority delivery"],
      ["Unlimited requests","Unlimited revisions","Fast delivery"]
    ],
    monthly: [
      ["Unlimited designs","Priority delivery","Pause anytime"],
      ["Unlimited requests","Priority queue","Multi-format"],
      ["Unlimited everything","Top priority","Dedicated support"]
    ]
  };

  function setMode(mode) {
    prices.forEach((p, i) => {
      p.style.opacity = "0";

      setTimeout(() => {
        p.textContent = mode === "monthly"
          ? p.dataset.monthly
          : p.dataset.project;
        p.style.opacity = "1";
      }, 150);

      const list = featureData[mode][i];
      features[i].innerHTML = list.map(item => `
        <li class="flex items-center gap-2">
          <span class="text-gray-400">✓</span>
          ${item}
        </li>
      `).join("");
    });

    // toggle UI
    if (mode === "monthly") {
      slider.style.left = "50%";
      monthlyBtn.classList.remove("text-gray-500");
      monthlyBtn.classList.add("text-gray-900");
      perBtn.classList.remove("text-gray-900");
      perBtn.classList.add("text-gray-500");
    } else {
      slider.style.left = "4px";
      perBtn.classList.remove("text-gray-500");
      perBtn.classList.add("text-gray-900");
      monthlyBtn.classList.remove("text-gray-900");
      monthlyBtn.classList.add("text-gray-500");
    }
  }

  perBtn.onclick = () => setMode("project");
  monthlyBtn.onclick = () => setMode("monthly");

  setMode("monthly");
</script>
